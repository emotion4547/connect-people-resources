

## Удаление тем и переключателя

Оставляем только стандартную синюю тему, убираем всё лишнее.

### Что будет сделано

1. **Удаление компонента ThemeSwitcher** -- файл `src/components/ThemeSwitcher.tsx` будет удалён
2. **Удаление контекста ThemeContext** -- файл `src/contexts/ThemeContext.tsx` будет удалён
3. **Очистка CSS** -- из `src/index.css` будут удалены блоки `.theme-ocean` и `.theme-luxury` (и их dark-варианты), останется только стандартная тема
4. **Обновление `src/pages/Index.tsx`** -- убрать импорт и использование `<ThemeSwitcher />`
5. **Обновление `src/App.tsx`** -- убрать `ThemeProvider` обёртку, оставив `AuthProvider` напрямую

### Технические детали

- Файл `src/components/ui/sonner.tsx` использует `useTheme` из `next-themes` (не из нашего контекста) -- его не трогаем
- CSS-переменные стандартной темы (`:root`) и её dark-вариант остаются без изменений
- Все компоненты продолжат работать, так как CSS-переменные стандартной темы определены в `:root`

